// Application-specific models for Picfluencer
// Models like ContentIdea, Script, Caption, CreatorProfile, etc. will go here

// Example future model:
/*
model ContentIdea {
  id          String   @id
  title       String
  description String?
  creatorId   String
  creator     User     @relation(fields: [creatorId], references: [id])
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
*/